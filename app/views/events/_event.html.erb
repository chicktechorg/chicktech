<div>
    <h3 id="event-title"><%= link_to event.name, event_path(event.id), :class => 'event-name' %></h3><h5 id="event-location"><%= event.city.name %></h5>    

  <h5 id="start-end">Starts on: <small><%= event.start.to_time.strftime("%b %-d, %Y @ %-I:%M%P") %></small></h5> <h5>Ends on: <small><%= event.finish.to_time.strftime("%b %-d, %Y @ %-I:%M%P") %></small></h5>
  <h5>Description:</h5> <span><p><%= event.description %></p></span>

  <% if can? :manage, event %>
    <div class="text-align">
      <div><%= link_to "Edit Event", edit_event_path(event) %> <%= link_to "Delete", event_path(event), :method => :delete, :data => { :confirm => 'Are you sure?' } %></div>
    </div>
  <% end %>
</div>
